// schemas/EpisodeSources.js

/**
 * EpisodeSources schema definition
 */
const EpisodeSources = {
    source: '', // Required: The source URL of the episode
    quality: '' // Required: The quality of the source (e.g., 720p, 1080p)
};

export default EpisodeSources;
